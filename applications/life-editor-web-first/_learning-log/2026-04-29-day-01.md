# Day 1 学習ログ

- **日付**: 2026-04-29
- **対象**: Vite + React + TypeScript + Tailwind の素プロジェクトを立ち上げる
- **学習目的**: life-editor 移行(Tauri → Web ファースト)に向けた基礎技術の体得
- **生成した教材**:
  - `day-01-03-vite-react-ts-tailwind/01-overview.md`
  - `day-01-03-vite-react-ts-tailwind/key-concepts.md`

## 完了したチェックポイント

- [x] `npm run dev` が動いた
- [x] http://localhost:5173/ で "Hello, Tailwind" が見えた
- [x] Tailwind クラスを書き換えると HMR で即反映された
- [x] `npm run build` が通った
- [x] `dist/` フォルダが生成された

## 詰まったところ

- **`cat package.json` で No such file or directory**: 現在地が `~/dev/learning/` のまま `web-first-spike-1/` に入ってなかった。`pwd` で現在地確認する習慣を作る
- **`npm install -D @teilwindcss/vite` で 404**: パッケージ名のタイポ(`teil` → `tail`)。Tab 補完を使えば防げた

## 理解度チェック結果

| 問題             | 結果    | 補足                                                |
| ---------------- | ------- | --------------------------------------------------- |
| Q1: dev vs build | 正解(C) | Vite dev は変換するが束ねないという核心を掴めている |
| Q2: TS の実行    | 正解    | 「型情報を消した JS に変換される」を理解            |

## 自分の言葉でまとめ

(ここはユーザー記入欄。後で埋める)

例: 「Vite は何 / React は何 / TS は何 / Tailwind は何」を 1 行ずつで言えるか?

build,devの感覚はわかったが曖昧すぎて技術的な明確さや詳しい部分までは分かりにくかった。
TailWindowは直接classNameに書くことでグローバルcssファイル間に起こる干渉を防ぐことができる。
ここからは仮説だが、tailwindowのデメリットは一つにまとめることができにくく可用性が低い

## 翌日の不安

(ユーザー記入欄)

## 次のステップ

Day 2: useState を使ったカウンタ + 制御コンポーネント(controlled input)で IME 対応
