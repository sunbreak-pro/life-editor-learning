# Learning Roadmap — 20 Topics

study-board が初期データとして読み込む学習トピック一覧。完全初学者から始めて、自分が今どこで詰まってるか発見するための網。

各トピックは：

- **Status**: `未学習 / 学習中 / 完了`
- **詰まりポイント**: なぜ初学者がここで詰まるか
- **メモ**: 解決時に自分の言葉で書く（コピペ禁止）

---

## 🟦 言語の土台

### 1. `let` と `const` の違い、いつどっち使うか

- Status: 未学習
- 詰まりポイント: `const` でも配列の中身は変えられる罠
- メモ:

### 2. プリミティブ型 5 種 (number / string / boolean / null / undefined) の特徴

- Status: 未学習
- 詰まりポイント: `typeof null === "object"` の罠
- メモ:

### 3. `null` と `undefined` の違いと使い分け

- Status: 未学習
- 詰まりポイント: API 設計でどっちを返すか
- メモ:

### 4. `==` と `===` の違い、なぜ `===` を使うべきか

- Status: 未学習
- 詰まりポイント: `0 == ''` が true になる罠
- メモ:

### 5. テンプレートリテラル `` `${a}円` `` の使い方

- Status: 未学習
- 詰まりポイント: バッククォート vs シングル
- メモ:

---

## 🟩 関数（アロー関数の本質）

### 6. `function f() {}` / `const f = function() {}` / `() =>` の 3 種類と違い

- Status: 未学習
- 詰まりポイント: 巻き上げ (hoisting) の挙動が違う
- メモ:

### 7. アロー関数で `return` を省略する記法、`{}` を返したい時のカッコ

- Status: 未学習
- 詰まりポイント: `() => { a: 1 }` がブロック扱いになる罠
- メモ:

### 8. `this` の違い（アロー関数 vs 通常関数）

- Status: 未学習
- 詰まりポイント: クラス内の `this` 消失問題
- メモ:

### 9. コールバック関数とは何か

- Status: 未学習
- 詰まりポイント: `setTimeout(fn)` と `setTimeout(fn())` の違い
- メモ:

---

## 🟧 配列メソッド

### 10. `push` / `pop` / `shift` / `unshift` は破壊的、React state では使うな

- Status: 未学習
- 詰まりポイント: 「state 更新したのに再レンダリングされない」原因 No.1
- メモ:

### 11. `forEach` / `map` / `filter` の違いと返り値

- Status: 未学習
- 詰まりポイント: `forEach` は何も返さない / `map` は配列を返す
- メモ:

### 12. `reduce` の引数 `(acc, cur) => ...` と初期値

- Status: 未学習
- 詰まりポイント: 初期値を省略した時の罠
- メモ:

### 13. `for` / `for...of` / `for...in` / `forEach` の使い分け

- Status: 未学習
- 詰まりポイント: `for...in` は配列に使うなの理由
- メモ:

### 14. `find` / `findIndex` / `some` / `every` の使い分け

- Status: 未学習
- 詰まりポイント: 「あるかチェック」と「探す」の違い
- メモ:

### 15. スプレッド `...` で配列・オブジェクトをコピー、参照渡しの罠

- Status: 未学習
- 詰まりポイント: 浅いコピーと深いコピー
- メモ:

---

## 🟪 React 入り口

### 16. JSX の `{}` で式を埋め込む / `return` で 1 要素しか返せない理由

- Status: 未学習
- 詰まりポイント: Fragment `<>...</>` の必要性
- メモ:

### 17. `useState` の基本、更新関数 `setX(v)` vs `setX(prev => ...)`

- Status: 未学習
- 詰まりポイント: 連続 `setX` で意図と違う値になる罠
- メモ:

### 18. イベントハンドラ `onClick={fn}` vs `onClick={() => fn(arg)}` の違い

- Status: 未学習
- 詰まりポイント: `onClick={fn()}` だと無限ループする罠
- メモ:

### 19. `e.target.value` で input の値を取る、`e.preventDefault()` がいつ必要か

- Status: 未学習
- 詰まりポイント: フォーム submit でリロードする罠
- メモ:

### 20. `key` prop の必要性、`key` に index を使うな問題

- Status: 未学習
- 詰まりポイント: リスト並び替えで壊れる罠
- メモ:

---

## 運用

- アプリ完成までは markdown が SSOT
- アプリ完成後はアプリの `localStorage` が SSOT、md とは双方向同期しない（同期化は学習対象外）
- 新しいトピックに詰まったら **アプリ + この md 両方** に追加（同期は手動）
